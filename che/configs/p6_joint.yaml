# p6_joint.yaml — GENERATED by che/scripts/make_phase6_configs.py.
# Do not hand-edit: test_phase6_configs.py re-derives every weight
# and fails on drift. Registered design: phase6_design_v2.md.
#
# JOINT-classic: all elements co-active.
#
# Realized marginals: A 1.0000  B 1.0000  co-occurrence 1.0000  no-element 0.0000
# Training severities {0.43, 0.70}, uniform. beta = 0.49 is HELD OUT
# (theta*) and must never appear here — enforced by test_locks.
env:
  grid_size: 64
  n_agents: 12
  horizon: 256
  obs_window: 9
  obs_version: 3
  n_food: 32

# Base theta: the locked constants. Every component states all four
# traced fields explicitly, so nothing here is inherited by accident.
theta:
  beta: 0.43
  kappa_A: 0.06
  lambda_0: 5.0e-5
  lambda_load: 4.0e-4
  f_weak: 0.15
  kappa_B: 1.0
  delta: 0.0
  r_comm: 16.0
  death_penalty: 0.5
  iota: 0.0
  sigma_s: 1.0
  eta: 0.5

mixture:
  components:
    - {name: joint_low, weight: 0.5, beta: 0.43, kappa_A: 0.06, kappa_B: 1.0, delta: 1.0}
    - {name: joint_high, weight: 0.5, beta: 0.7, kappa_A: 0.06, kappa_B: 1.0, delta: 1.0}

train:
  n_envs: 256
  rollout_len: 128
  n_minibatches: 4
  n_epochs: 4
  ckpt_interval: 50
  ckpt_max_to_keep: 11  # Gamma(t) window: final half of training
